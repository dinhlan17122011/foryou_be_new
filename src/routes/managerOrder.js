// import { Router } from 'express';
// const router = Router();
// import { getOrders, viewOrderDetails, updateOrderStatus, deleteOrder } from '../controllers/orderConfirmation.js';

// // Route để lấy danh sách đơn hàng
// router.get('/', getOrders);

// // Route để xem chi tiết đơn hàng
// router.get('/view/:id', viewOrderDetails);

// // Route để cập nhật trạng thái đơn hàng
// router.post('/updateStatus/:id', updateOrderStatus);

// // Route để xóa đơn hàng (nếu cần)
// router.post('/delete/:id', deleteOrder);

// export default router;
