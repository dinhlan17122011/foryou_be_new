<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cake Details</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background: white;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        h1 {
            text-align: center;
            color: #333;
        }
        .cake-details {
            margin-top: 20px;
        }
        .cake-details img {
            max-width: 300px;
            border-radius: 4px;
        }
        .cake-details ul {
            list-style: none;
            padding: 0;
        }
        .cake-details li {
            padding: 8px;
            border-bottom: 1px solid #ddd;
        }
        .back-btn {
            display: inline-block;
            margin-top: 20px;
            padding: 8px 16px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 4px;
        }
        .back-btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <%- include('./navbar') %>
    <div class="container">
        <h1>Cake Details</h1>
        <div class="cake-details">
            <img src="<%= cake.image || 'no-image.jpg' %>" alt="Cake Image">
            <h2><%= cake.name || 'No Name' %></h2>
            <p><strong>Description:</strong> <%= cake.describe || 'No Description' %></p>
            <p><strong>Price:</strong> <%= cake.price || 'N/A' %> VNĐ</p>
            <p><strong>Category:</strong> <%= cake.category ? cake.category.name : 'No Category' %></p>
            <p><strong>Code:</strong> <%= cake.code || 'No Code' %></p>
            
            <% if (cake.size && cake.size.length > 0) { %>
                <h3>Sizes:</h3>
                <ul>
                    <% cake.size.forEach(size => { %>
                        <li>
                            <strong>Size:</strong> <%= size.size %> |
                            <strong>Price:</strong> <%= size.price %> VNĐ |
                            <% if (size.image) { %>
                                <img src="<%= size.image %>" alt="Size Image" style="max-width: 40px;">
                            <% } %>
                        </li>
                    <% }); %>
                </ul>
            <% } else { %>
                <p>No Sizes Available</p>
            <% } %>
        </div>
        <a href="/cake" class="back-btn">Back to Cake List</a>
    </div>
</body>
</html>
