<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
</head>
<body>
    <h1>Welcome to your Dashboard, <%= user.name %>!</h1>
    
    <% if (user.role === 'admin') { %>
        <div>
            <h2>Admin Section</h2>
            <p>Only accessible by admins. Here you can manage users, view reports, etc.</p>
        </div>
    <% } else { %>
        <div>
            <h2>User Section</h2>
            <p>Welcome, User! You can view your order history and update your profile here.</p>
        </div>
    <% } %>
</body>
</html>
